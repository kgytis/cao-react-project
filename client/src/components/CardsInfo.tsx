type CardsInfoProps = {
  key?: string;
  data: object;
};

const CardsInfo = ({ ...props }: CardsInfoProps) => {
  //   const { data } = props;
  return <div>CardsInfo</div>;
};

export default CardsInfo;
